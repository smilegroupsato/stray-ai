# Codex Work Order｜Observed Venue Map Cyberpunk Completion

Issue: `smilegroupsato/stray-ai#45`

## Role

Complete the remaining visual surface in Cyberpunk Visual Pass v1 by bringing the existing Observed Venue Map into the shared Stray AI terminal identity.

Work on a new branch:

```text
codex/observed-venue-map-cyberpunk-completion
```

The checked-in work order is the complete specification. Do not fetch the GitHub issue or other network resources.

Do not merge. Commit locally and report the commit SHA, changed files, tests, and remaining browser validation.

## Read first

- `README.md`
- `registry/current_board.yml`
- `src/stray_ai/brand.py`
- `src/stray_ai/report_map.py`
- `src/stray_ai/report_source_archive.py`
- `src/stray_ai/report_archive.py`
- `tests/test_report_map.py`
- related report UI and safety tests

## Current state

Current Board, Visit Report archive, and individual Visit Report pages already use:

- shared `terminal-shell`
- shared `title-zone`
- shared inline Stray AI mark
- shared SVG data-URI favicon
- CSS-only grid, scanline, and low-level noise
- cyan/magenta/yellow visual hierarchy

Observed Venue Map still defines an independent legacy theme inside `src/stray_ai/report_map.py` and does not use the shared brand helpers.

## Required implementation

### Shared identity

Update `render_observed_map` so `map.html` uses:

- `cyberpunk_css()`
- `favicon_link_html()`
- `inline_title_mark_svg()`
- `main.terminal-shell.observed-map-shell`
- `header.title-zone`
- the existing title and explanatory text, preserving meaning

### Map-specific presentation

Apply the same terminal identity while keeping the map readable:

- summary metrics as telemetry modules
- each Venue as a distinct observation sector/panel
- Venue number and source coordinates as compact terminal metadata
- SVG graph enclosure with stronger depth and clear overflow behavior
- graph nodes using cyan/magenta/yellow semantics
- entrance and terminal states must remain distinguishable without color alone
- edges and traversal counts remain legible
- route table and observed-page table become evidence tables/modules
- external source links remain recognizable as links
- local report links remain relative

### Semantic and safety preservation

Do not change:

- `build_observed_map`
- Venue grouping
- node identity
- edge aggregation
- route ordering
- observed-only semantics
- source-coordinate construction
- commit-fixed external URLs
- relative local report URLs
- the rule against inferred geography or inferred travel
- output filename `map.html`
- archive map-link behavior

No Current Board, Visit, wake, Request, snapshot, scheduler, state, memory, Trace, or historical data changes.

## Constraints

Use only:

- Python-generated static HTML
- CSS
- existing dependency-free inline SVG mark

Do not add:

- JavaScript
- buttons or forms
- operational controls
- PNG/JPEG/WebP/GIF/font files or other binary assets
- external fonts, CDN, remote images, or network calls
- new routes
- reverse links to Current Board

## Responsive and accessibility

- no horizontal page overflow at 320px, except intentional horizontal scrolling inside the SVG/table wrappers
- preserve readable Japanese/English text
- retain semantic heading order
- visible keyboard focus for links
- graph SVG retains `role="img"` and title labeling
- entrance and terminal differences must remain understandable through border/pattern/label treatment, not color alone

## Tests

Add or update `tests/test_report_map.py` to assert at least:

- `main.terminal-shell.observed-map-shell`
- `header.title-zone`
- inline `svg.stray-mark` before the map title
- SVG data-URI favicon
- shared terminal/grid/scanline markers
- stable map-specific classes for summary, Venue sectors, SVG graph, and evidence tables
- graph still has role/title accessibility
- source links remain exact and external links retain safe attributes
- local report links remain relative
- no Current Board reverse link
- no `<script`, `<button`, `<form`, `javascript:`, `file://`, `/srv/`, `snapshot_root`, or `brain_command`
- no `url(http` external CSS asset
- source-aware archive still writes and links `map.html`
- existing observed-map aggregation tests remain unchanged and passing

Run:

```bash
.venv/bin/python -m compileall -q src tests scripts
.venv/bin/python -m pytest -q
bash -n scripts/setup_devbox.sh scripts/devbox/*.sh
git diff --check
git diff --numstat
```

## Safety and side-effect boundary

During implementation:

- do not run wake checks
- do not fetch or create snapshots
- do not read Venue content
- do not create, approve, cancel, claim, or execute Visit Requests
- do not run a Visit
- do not run a scheduler
- do not regenerate or publish persistent `stray-001` report HTML
- do not mutate historical Visit JSON, wake records, memory, Trace, or state
- do not push, open a PR, or merge

Synthetic temporary HTML generation inside tests is allowed.

## Completion

Finish only after implementation, tests, diff review, and local commit are complete.

Report:

- branch
- commit SHA
- changed files
- test results
- confirmation that no binary files were added
- confirmation that persistent data and published HTML were not changed
- remaining map regeneration and desktop/mobile browser checks
