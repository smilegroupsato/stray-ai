# Return Handoff｜Stray-002 First Genuine Runtime Rummage

- ページ作成日時：2026-07-25 10:24 JST
- 最終更新日時：2026-07-25 10:24 JST

## Return

`stray-002` completed its first genuine command-brain repository rummage and
returned to its home shelf gap. The successful run was performed once, after a
non-persistent survey diagnostic established the required model context.

## Formal run evidence

- Source commit: `9dc6b9ea3de3a5bae061e5bba73c103b39cdedfe`
- Started: `2026-07-25T10:16:18+09:00`
- Ended: `2026-07-25T10:19:02+09:00`
- Backend: `command`
- Brain model: `stray-qwen3.5-9b-16k`
- Protocols:
  - `stray-rummage-survey-v1`
  - `stray-rummage-reflection-v1`
- Deep read: `registry/current_board.yml`
- Formal record:
  `/srv/sgos/data/stray-ai/agents/stray-002/rummages/2026-07-25_101618.json`
- Process exit: `0`

## Returned state

```yaml
status: resting
current_location: damp-underground-library-shelf-gap
document_rummage_count: 2
runtime_rummage_count: 1
llm_rummage_count: 1
visit_count: 0
```

No Visit, wake, scheduler, repository-content change, or Trace was created.
`stray-001` was outside the runtime's write boundary.

## Failure resolved before the formal run

The original model used an effective 4,096-token context. The fixed
seven-document survey consumed 4,020 prompt tokens, leaving 76 completion
tokens. The response therefore ended with `finish_reason: length` inside the
first JSON string. Increasing `STRAY_LLM_MAX_TOKENS` alone did not change that
total context limit.

A derived local model was then created from `qwen3.5:9b` with:

```text
PARAMETER num_ctx 16384
```

A non-persistent survey completed with valid JSON under that model. The formal
rummage then completed without retry and wrote the record above.

## Repository return

This return standardizes the successful condition through:

- a trusted checked-in Modelfile
- an explicit, no-auto-pull model setup command
- post-create `num_ctx` verification
- a devbox launcher for that setup command
- the verified derived model as the rummage launcher's default
- tests that keep model setup separate from rummage execution

## Held decisions

The following remain intentionally unperformed and require separate human
decisions:

- scheduling or periodic rummage
- automatic retry
- automatic report generation or publication
- Current Board or other public/private read surface integration
- changing the fixed document route

## Update History

- 2026-07-25 10:24 JST：Recorded the first genuine runtime rummage, resolved 4K context exhaustion, and returned the remaining decisions.
