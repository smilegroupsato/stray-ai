# Codex Work Order｜Current Board / Visit Report cyberpunk UI refinement

Issue: `smilegroupsato/stray-ai#42`

## Role

You are the implementation agent for one bounded presentation refactor in `smilegroupsato/stray-ai`.

Work on a new branch named:

```text
codex/current-board-visit-report-cyberpunk-ui
```

Do not merge the PR. Return the branch, commit SHA, PR number, tests, generated-file impact, and any remaining browser checks.

## Read first

- `README.md`
- `docs/current-board.md`
- `docs/visit-report-index.md`
- `docs/visit-report-multiple-individuals.md`
- `registry/current_board.yml`
- `src/stray_ai/current_board.py`
- `src/stray_ai/report.py`
- `src/stray_ai/report_archive.py`
- `src/stray_ai/report_presentation.py`
- report augmentation/navigation modules used by `generate_source_aware_archive`
- related tests
- Issue #42

## Implement

### A. NOW purpose

1. Add `now.purpose` as a dedicated scalar string in `registry/current_board.yml`.
2. Remove the purpose entry from `now.notes`.
3. Validate that `purpose` is a non-empty scalar string; mappings/lists fail closed.
4. Render purpose as a standalone paragraph under the NOW title and before the stage/action/authorization cells.
5. It must not render as a bullet or Python representation.

### B. Completed foundation hierarchy

1. Extend the Current Board schema only as much as needed to express a group with child records.
2. `完了済み基盤` is the group/container.
3. Each completed foundation is a subordinate child with title and one-line purpose.
4. Render separate semantic/classes for group and child, for example:
   - `.board-group`
   - `.board-group-title`
   - `.board-children`
   - `.board-child`
5. Preserve backward compatibility for current string and simple mapping items in other sections.
6. Add fail-closed tests for malformed children.

### C. Shared Stray AI mark and favicon

Create a shared helper module, preferably `src/stray_ai/brand.py`, with no external dependencies.

The mark is an inline SVG:

- abstract orb / cocoon
- irregular concentric contours
- central magenta node/glow
- cyan lower crescent/highlight
- no letters
- readable at favicon scale
- deterministic static markup
- accessible decorative usage with `aria-hidden="true"` where appropriate

Expose helpers for:

- inline title mark SVG
- SVG favicon data URI
- optional shared cyberpunk CSS tokens/snippet

Use the same mark:

- before the Current Board page title
- before the Visit Report archive/index title area
- before the individual Visit Report title area
- in a `<link rel="icon" href="data:image/svg+xml,...">` on all those pages

No remote image/font/CDN.

### D. LIVE to Visit Report

Add exactly one read-only text link in Current Board LIVE:

```html
<a href="../index.html">Stray AI · 訪問レポートを見る</a>
```

Requirements:

- relative URL exactly `../index.html`
- no button role/class suggesting an action control
- no JavaScript
- do not add a reverse Current Board link to Visit Report
- keep Visit Report navigation boundaries otherwise unchanged

### E. Cyberpunk palette

Apply a restrained shared visual language to Current Board and Visit Report archive/individual pages.

Suggested tokens:

```css
--bg-0: #05070b;
--bg-1: #090d14;
--panel: rgba(8, 14, 22, .92);
--panel-2: rgba(13, 21, 32, .92);
--text: #e8fbff;
--muted: #8da7b3;
--cyan: #39f6ff;
--magenta: #ff4fd8;
--yellow: #ffe66d;
--line: rgba(57, 246, 255, .28);
--line-magenta: rgba(255, 79, 216, .25);
```

Direction:

- near-black background
- cyan primary accents
- magenta secondary accents
- yellow only for NOW/warning emphasis
- CSS-only subtle grid/scanline texture
- thin neon borders, restrained shadows/glow
- high readability; avoid decorative noise behind body copy
- responsive layout preserved
- no animation required
- no JavaScript

Update both base renderers and ensure BeautifulSoup localization/augmentation preserves the theme, favicon, and mark.

### F. Tests

Add/update tests to assert:

- scalar `now.purpose` renders as prose
- mapping/list purpose fails closed
- purpose does not appear in `.now-notes`
- completed foundations are nested under one distinct group container
- child title/detail are present
- mark SVG precedes titles on Current Board, archive/index, and individual Visit pages
- favicon is an SVG data URI on all target page types
- Current Board contains exactly one `href="../index.html"`
- Visit Report contains no Current Board reverse link
- cyberpunk tokens/classes exist on both surface types
- no `<script`, `<button`, `<form`, `javascript:`, `file://`, `/srv/`, `snapshot_root`, or `brain_command`
- all existing report localization and responsive tests still pass

Run:

```bash
python -m compileall -q src tests scripts
pytest -q
bash -n scripts/setup_devbox.sh scripts/devbox/*.sh
```

## Safety / no side effects

During implementation and CI:

- do not run wake checks
- do not fetch snapshots
- do not read Venue content
- do not create/approve/cancel/execute Requests
- do not run a Visit
- do not regenerate persistent `stray-001` reports unless performing the explicit final devbox validation after code review
- do not mutate historical Visit JSON, wake records, memory, Trace, or state
- no scheduler or automatic publication

## PR

Open a PR to `main` with:

- `Closes #42`
- concise visual/schema summary
- explicit safety boundary
- full test result
- list of files changed
- note that persistent devbox publication/browser validation remains separate unless completed

Do not merge.
